import styled from 'styled-components/native';
import { FeaturedTabIconProps } from './FeaturedTabIcon.interface';

export const FeaturedTabIcon = styled.View<Pick<FeaturedTabIconProps, 'myProp'>>`
  /* Add styles here */
`;
