import styled from 'styled-components/native';
import { ProfileScreenProps } from './ProfileScreen.interface';

export const ProfileScreen = styled.View<Pick<ProfileScreenProps, 'myProp'>>`
  /* Add styles here */
`;
